%h1 #{@restaurant.name} menus
- @menus.each do |menu|
  %p= menu.name
  %p= link_to "Edit #{menu.name} Menu", edit_owners_restaurant_menu_path(@restaurant, menu)
%div
  = link_to 'Create Menu', new_owners_restaurant_menu_path(@restaurant)
